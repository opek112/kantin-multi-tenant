# Module Revision Log

Revisi dokumen modul hanya dilakukan bila ada bukti (lihat protokol revisi pada prompt tugas).
Base dokumen aktual: `Modul_..._v7.docx`. Target revisi kumulatif: **`Modul_..._v8.docx`** (dikonfirmasi pemilik 2026-08-16, **sudah dibuat** di root repo).

Status: `DIUSULKAN` (baru catatan) · `DITERAPKAN` (kode teruji + DOCX diperbarui + inspeksi visual).

| ID | Modul/Tahap | Temuan | Bukti | Resolusi | Status |
|---|---|---|---|---|---|
| DOC-01-001 | Global / parameter | Prompt menyebut base `v4` → revisi `v5`, tetapi berkas nyata di `references/` adalah **v7** dan tidak ada v4/v5. | `ls references/` hanya memuat `..._v7.docx`. | **DISEPAKATI**: base **v7**, revisi kumulatif ditulis ke `..._v8.docx` (konfirmasi pemilik 2026-08-16). | DITERIMA |
| DOC-01-006 | Modul 1 / Tahap 5 | `php artisan install:broadcasting` diasumsikan menyelesaikan setup realtime, tetapi langkah build npm-nya gagal di lingkungan non-interaktif (TTY) dan tidak menuliskan `REVERB_*` env maupun memasang `laravel-echo`/`pusher-js`. | Output installer: `TTY mode requires /dev/tty`; `npm run build` gagal resolve `laravel-echo`; `.env` tanpa `REVERB_*` → `package:discover` error broadcaster reverb. | Tambah langkah eksplisit: `npm install laravel-echo pusher-js` dan isi `REVERB_APP_ID/KEY/SECRET` di `.env`/`.env.example`. Terverifikasi (`package:discover` bersih, build OK). Perbarui Tahap 5 & troubleshooting. | DITERAPKAN (v8) |
| DOC-01-002 | Global / basis desain | Modul menyatakan basis "SRS v1.1", input tersedia adalah **SRS v2**. | Header modul baris "Basis desain: SRS v1.1"; berkas `SRS_..._ISO29148 v2.docx`. | Audit diff SRS v1.1→v2 sebelum Modul 3 (data). Perbarui RTM & ketertelusuran ke SRS v2 (otoritas lebih tinggi). | DITERAPKAN (v8) |
| DOC-01-003 | Global / strategi Git | Modul: `main → develop → feat/week-NN`, evidence minimal, tanpa PR/tag/release. Prompt (otoritas lebih tinggi): `main` + `feat/pertemuan-NN-slug` + annotated tag `pertemuan-NN` + GitHub Release + CI required checks. | Bagian "Strategi Branch dan Evidence" modul vs kontrak GitHub pada prompt. | Ganti strategi ke model prompt; Tahap 7 tiap modul mencakup PR→CI→merge→tag→release; `git worktree` untuk membuka versi berdampingan. | DITERAPKAN (v8) |
| DOC-01-004 | Modul 1 / Tahap 4 & cuplikan | Modul memakai `DB_PORT=3307`, `DB_DATABASE=kantin_multi_tenant`, `REDIS` port `6380`. Environment nyata (kredensial pengguna, terverifikasi live): `DB_PORT=3306`, `DB_DATABASE=kantin`, `DB_USERNAME=kantin`, Redis `6379`. | `mysql -h127.0.0.1 -P3306 -ukantin` → MariaDB 11.4.5, db `kantin` ada; `redis-cli -p 6379 ping` → PONG; `-p 6380` connection refused. | Gunakan konfigurasi nyata pengguna (otoritas tertinggi). Lihat [ADR-0002](adr/0002-database-redis-connection.md). Perbarui Tahap 4, cuplikan `.env.example`, tabel troubleshooting port. | DITERAPKAN (v8) |
| DOC-01-005 | Modul 1 / intro vs Tahap 4 | Zona waktu tidak konsisten: intro menyebut `Asia/Jakarta`, Tahap 4 menyebut `Asia/Makassar`. | Baris intro §mode produksi vs Tahap 4. | Simpan UTC di DB; presentasi `Asia/Jakarta` (WIB). Lihat [ADR-0003](adr/0003-timezone.md). Seragamkan teks modul. | DITERAPKAN (v8) |

| DOC-02-001 | Modul 2 / Tahap 3 & cuplikan | Modul memakai `{tenant:slug}`/`{canteen:slug}` + `scopeBindings()`, tetapi model Tenant/Canteen baru dibuat pada Modul 3 dan isolasi/binding pada Modul 4 — jika ditulis di Modul 2, implicit binding gagal (model belum ada). | Route group tenant/customer dibuat di Modul 2 dengan parameter string; `php artisan route:list` menampilkan `tenant/{tenant}`, `kantin/{canteen}` tanpa error. | Modul 2 membangun kerangka route (nama+middleware+placeholder); `scopeBindings` + model binding di-wire pada Modul 4. Perbarui teks Tahap 3 agar eksplisit menyatakan binding ditunda. | DIUSULKAN |

> Catatan: edit DOCX v8 **sudah diterapkan** (textual revisions + Lampiran G change-log & output terminal terverifikasi Modul 1). Inspeksi visual: halaman sampul diverifikasi via Quick Look (SRS v2, Versi 8.0); render penuh per halaman `TIDAK DIUJI` (LibreOffice/Word-automation tidak tersedia di environment ini) — buka v8 di Word untuk verifikasi akhir. Output terminal Modul 2–14 ditambahkan bertahap saat modul diimplementasikan (tanpa mengarang).
